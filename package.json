{
  "name": "local-ai-image-detector",
  "version": "1.0.0",
  "description": "Chrome extension that detects AI-generated images entirely inside the browser.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test \"eval/tests/*.test.mjs\"",
    "eval": "node eval/run-benchmark.mjs",
    "parity": "node eval/parity.mjs"
  },
  "devDependencies": {
    "puppeteer": "^23.11.1"
  }
}
